export interface Formacion {
    id?: number; //Cuando creamos el id podría no venir
    titulo: string;
    institucion: string;
    periodo: string;
    fotoEstudio: string;
    certificado: string;
}